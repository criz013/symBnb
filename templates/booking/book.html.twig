{% extends 'base.html.twig' %}

{% block title %}Réserver l'annonce: {{ ad.tilte }} !{% endblock %}

{% block body %}
    <div class="container">
    <h1>Réserver l'annonce: {{ ad.tilte }}</h1>
    <p>Vous êtes sur le point de réserver le bien de <strong>{{ ad.getFullName }}</strong>, dite nous tous, on s'occuper du reste!
    
    </p>
    {{ form_start( form ) }}
    <div class="alert alert-light">
        <h2>Commencez par nous dire quelles sont les dates qui  vous intérressent !</h2>
        <div class="row">
            <div class="col">{{ form.startDate }}</div>
            <div class="col">{{ form.endDate }}</div>
        </div>
    </div>
    <h4>Montant du séjour: <span id="amout">..</span> &euro;</h4>
    <span id="days">0</span> nuit(s) à {{ ad.price }} / nuit

    {{ form_end( form ) }}
    </div>
{% endblock %}
